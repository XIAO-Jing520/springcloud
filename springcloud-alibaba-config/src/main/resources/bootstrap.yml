# nacos配置
server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.81.226:8848 #Nacos服务注册中心地址
      config:
        server-addr: 192.168.81.226:8848 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: DEFAULT_GROUP
        namespace: 291a3917-148e-4e36-b80d-e92213958508


# ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension} 和下面对应
# nacos-config-client-dev.yaml

# nacos-config-client-test.yaml   ----> config.info